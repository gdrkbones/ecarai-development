import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#9AD14B",
      lightShadow: "rgba(154,209,75,0.5)",
      main: "#749D38",
    },
    secondary: {
      light: "#824BD1",
      main: "#61389D",
    },
    dirt: {},
  },
});
export default theme;
